{
  "name": "sdlc-workflow",
  "owner": {
    "name": "Evan",
    "email": "e2438927@gmail.com"
  },
  "metadata": {
    "description": "AI-driven SDLC automation pipeline (proposal → apply → qa → accept → pr) with dual-model review gates",
    "version": "1.0.0"
  },
  "plugins": [
    {
      "name": "sdlc-full",
      "description": "Full SDLC workflow suite: five-stage pipeline (proposal → apply → qa → accept → pr) plus doit/mini/worktree, with optional dual Codex review gates and Playwright browser QA",
      "source": "./",
      "strict": true,
      "commands": [
        "./commands"
      ]
    }
  ]
}
